# =========================
# Basic Site Settings
# =========================
Options -Indexes
DirectoryIndex index.php

<IfModule mod_rewrite.c>
  RewriteEngine On

  # Mevcut dosya/klasör varsa direkt aç
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # Pretty URL fallback (şu an gerek yoksa yorumda bırak)
  # RewriteRule ^ index.php [L,QSA]
</IfModule>

# =========================
# Security Headers
# =========================
<IfModule mod_headers.c>
  Header always set X-Content-Type-Options "nosniff"
  Header always set X-Frame-Options "SAMEORIGIN"
  Header always set Referrer-Policy "strict-origin-when-cross-origin"
  Header always set X-XSS-Protection "1; mode=block"
</IfModule>

# =========================
# Block Sensitive Files
# =========================
<FilesMatch "(^\.|composer\.(json|lock)|package\.json|yarn\.lock|pnpm-lock\.yaml|.*\.md|.*\.log|.*\.bak|.*\.sql)$">
  Require all denied
</FilesMatch>

# =========================
# Optional: Force HTTPS
# (SSL hazırsa aç, değilse kapalı bırak)
# =========================
# <IfModule mod_rewrite.c>
#   RewriteCond %{HTTPS} !=on
#   RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
# </IfModule>

# =========================
# Plesk PHP Handler (opsiyonel)
# Eğer sunucu izin veriyorsa PHP sürümünü buradan zorlayabilirsin.
# Bunlardan sadece 1 satırı aç.
# =========================
# AddHandler application/x-httpd-php84 .php
# AddHandler application/x-httpd-php82 .php
# AddHandler application/x-httpd-plesk-php84-fastcgi .php